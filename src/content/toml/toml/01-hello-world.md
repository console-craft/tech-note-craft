---
category: TOML
order: 1
---

# Hello, World!

TOML is a readable configuration format built from key-value pairs and tables.

```toml
[greeting]
message = "Hello, World!"
enabled = true
```

The `[greeting]` table groups related configuration values together.
