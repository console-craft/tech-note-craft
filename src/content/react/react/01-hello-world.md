---
category: React
---

# Hello, World!

React components are functions that return UI.

```jsx
export function App() {
  return <h1>Hello, World!</h1>
}
```

The component returns JSX, which React renders as an HTML heading.
