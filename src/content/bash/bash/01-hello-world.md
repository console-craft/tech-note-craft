---
category: Bash
---

# Hello, World!

The smallest Bash script prints text to standard output.

```bash
#!/usr/bin/env bash

echo "Hello, World!"
```

`echo` writes the string, and the shebang tells the shell how to run the script when executed directly.
