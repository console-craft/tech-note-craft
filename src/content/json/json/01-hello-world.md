---
category: JSON
order: 1
---

# Hello, World!

JSON stores data as objects, arrays, strings, numbers, booleans, and null.

```json
{
  "message": "Hello, World!"
}
```

This object has one key, `message`, whose value is a string.
