---
category: YAML
---

# Hello, World!

YAML is often used for configuration because it is easy to read.

```yaml
greeting:
  message: Hello, World!
  enabled: true
```

Indentation creates nested data, so `message` and `enabled` belong to `greeting`.
