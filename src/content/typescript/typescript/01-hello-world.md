---
category: TypeScript
---

# Hello, World!

TypeScript adds static types to JavaScript.

```typescript
const message: string = "Hello, World!"

console.log(message)
```

The `string` annotation tells TypeScript what kind of value `message` should hold.
