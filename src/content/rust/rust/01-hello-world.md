---
category: Rust
---

# Hello, World!

Rust programs begin in a `main` function.

```rust
fn main() {
    println!("Hello, World!");
}
```

`println!` is a macro that writes formatted text followed by a newline.
