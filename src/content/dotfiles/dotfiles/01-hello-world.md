---
category: Dotfiles
order: 1
---

# Hello, World!

Dotfiles configure command-line tools through files stored in your home directory.

```bash
# ~/.bashrc
alias hello='printf "Hello, World!\n"'
```

After reloading the shell, running `hello` expands to the configured command.
